-- Run this manually, connected to the default "postgres" database
-- (CREATE DATABASE cannot run inside a script targeting the db being created).
--
-- psql -U postgres -h 127.0.0.1 -p 5432 -f sql/01_create_database.sql

CREATE DATABASE ai_engineer_embeddings;
